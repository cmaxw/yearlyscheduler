<%= form_for(@label_color) do |f| %>
  <% if @label_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@label_color.errors.count, "error") %> prohibited this label_color from being saved:</h2>

      <ul>
      <% @label_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
